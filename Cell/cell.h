/*
class Cell
{
    size_t * d_distributions;
    size_t * d_new_distributions;

    int * d_velocity_set; // The domain's velocity set
    size_t * d_distribution_weights; // weights corresponding to the velocity set

public:
    Cell();
    ~Cell();

    void initializeDistributions(size_t *distributions, Cell *neighbours);

    void stream();
    void collide();

    int * velocity();

};
*/